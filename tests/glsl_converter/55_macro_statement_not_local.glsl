#define k =min(length(p)-a,p.y+a);p+=j
void mainImage(out vec4 fragColor, in vec2 fragCoord)
{
    float r = 1.0, a = 0.1;
    vec3 p = vec3(fragCoord, 0.0), j = vec3(0.01);
    r k;
    fragColor = vec4(r, p.x, p.y, 1.0);
}
